# Local-only notes (`docs/_private/`)

This folder is **gitignored as a whole** except for this `README.md`. Put any maintainer-only drafts here (demo scripts, pitch notes, submission copies, etc.): they will **not** be pushed to GitHub.

Use normal Markdown filenames; no need to match old paths from the public tree.
